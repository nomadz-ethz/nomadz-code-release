# GameController headers ###

set(GAME_CONTROLLER_INCLUDE_DIRS
  "${CMAKE_CURRENT_SOURCE_DIR}"
  PARENT_SCOPE)

install(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/"
  DESTINATION "include"
  FILES_MATCHING
  PATTERN "*.h"
)
