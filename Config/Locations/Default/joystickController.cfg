headPanBound = [-0.8, 0.0, 0.8];   //lower bound, zero pos, upper bound
headTiltBound = [-0.5, -0.3, 0.1]; //lower bound, zero pos, upper bound
headSpeedBound = [0.5, 2.0, 3.0]; //lower bound, init val, upper bound
headSpeedStep = 0.1;

walkingSpeedBound = [180., 220., 240.]; //lower bound, init val, upper bound
walkingSpeedPercBackward = 50.;
walkingSpeedPercYDir = 30.;
walkingSpeedPercRot = 5.;
walkingSpeedStep = 1.;

frequencyButtons = 20; //Trigger button every [frequencyButtons]th frame update
frequencyData = 10; //Collect data every [frequencyData]th frame update

pathData = "/home/nao/data";

printOuts = false;

socketIP = "10.0.33.11";

useJoystickControl = false;
